/**
 * A Creator constructor object.
 */
export interface CreatorConstructor {
  /**
   * The creator's Steam name.
   */
  steamName?: string;

  /**
   * The creator's Steam name.
   */
  steam_name?: string;

  /**
   * The creator's SteamID3. It's assigned in the Project Arrhythmia editor.
   */
  steamId?: number | string;

  /**
   * The creator's SteamID3. It's assigned in the Project Arrhythmia editor.
   */
  steam_id?: number | string;
}
